# Copy this file to config-local.fish and add your real env vars.
# config-local.fish is gitignored — never commit it.

# Examples (delete and replace with the env vars you actually need):
set -x EXAMPLE_API_KEY     ""
set -x EXAMPLE_CLIENT_ID   ""
set -x EXAMPLE_PRIVATE_KEY ""
